import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import CustomText from '../utils/CustomText';

const SplashScreen = () => {
  return (
    <View>
      <CustomText>selam 21</CustomText>
    </View>
  );
};

export default SplashScreen;

const styles = StyleSheet.create({});
